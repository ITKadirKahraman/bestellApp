export function testArray(array) {
    return array = [
        {
            image: array[0].image,
            name: array[0].name,
            price: array[0].price,
            description: array[0].description,
            added: array[0].added,
        }
    ]
}